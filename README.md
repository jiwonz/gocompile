gocompile provides QUICKSTART which contains some OS presets we usally use.

# Usage
```sh
gocompile <.go file to build compile> <goos:optional> <goarch:optional>
```

## Using QUICKSTART
```sh
gocompile <.go file to build compile>
```
